<resources>

    <!-- Base application theme. -->
    <style name="SimpleClassAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- 开启动画效果 -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:inAnimation">@android:anim/decelerate_interpolator</item>
        <item name="android:outAnimation">@android:anim/decelerate_interpolator</item>
        <item name="android:activityOpenEnterAnimation">@android:anim/decelerate_interpolator</item>
        <item name="android:activityOpenExitAnimation">@android:anim/decelerate_interpolator</item>
        <item name="android:activityCloseEnterAnimation">@android:anim/decelerate_interpolator
        </item>
        <item name="android:activityCloseExitAnimation">@android:anim/decelerate_interpolator</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
